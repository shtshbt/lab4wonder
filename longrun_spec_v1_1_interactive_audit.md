# Lab4Wonder v1.1.0 Interactive Quality Release

## Goal

183本のHTMLアプリを一つずつ監査し、子どもが主画面へ触れるだけで題材固有の変化を起こせる、見た目から対象と因果が分かる、繰り返し遊んだ後にパラメータの意味を探究できるプラットフォームへ改修する。検証後にConventional Commitsで記録し、GitHubへpushして`v1.1.0`をtagする。

## Non-negotiable Product Principles

1. 入口は説明文や再生ボタンではなく、主画面へのタップまたはドラッグである。
2. 反応は即時で、色・形・位置・運動の大きな変化として見える。
3. 単なる波紋、トースト、数値増加だけを直接操作の合格としない。
4. タップ位置や操作方法を変えると結果が変わり、何度も試す理由がある。
5. 主役の生物・物体・現象は、タイトルを隠しても何を表すか判別できる。
6. 色、記号、専門語、パラメータは、画面上の対象と観察結果へ対応づける。
7. 入口の遊びを妨げず、興味が出た後に詳細パラメータ、比較、模型の限界へ進める二層構造にする。
8. 失敗後に画面をロックせず、再試行または複数試料の比較を続けられる。

## Existing Evidence

- 183/183本について、iPad相当の初期・再生・リセット・タップ・ドラッグ後スクリーンショットを取得済み。
- JavaScript例外は0本。
- 共通位置へのタップまたはドラッグで画面変化を検出できたのは21/183本。162本は両方とも変化なし。
- 全183本の図はSVGではなくCanvas描画。以下では「SVG品質」の指摘をCanvas上の図形品質として扱う。
- 監査データは作業中に`/tmp/lab4wonder_audit/`へ生成されている。リポジトリには要約監査表だけを保存する。

## Required Audit Deliverables

1. 183本すべてを含む`docs/app_quality_audit_v1_1.md`。
2. 各アプリについて、直接操作、即時反応、視覚判別、反復性、探究導線、用語説明、判定、改修内容または適用除外理由を記録する。
3. 自動検査だけで合格させず、初期主画面と操作後画面を目視確認する。
4. ゲーム、描画、楽器、エディタなど既に直接操作が本体のアプリは、題材に合う操作であることを証拠付きで保持する。
5. A（入口なし／判別不能）、B（直接操作はあるが弱い）、C（入口は十分で深掘り不足）、Passの順で改修する。

## Explicit Priority Apps

- `ant-colony.html`: 巣を広がりやすくし、アリを点ではなく判別できる姿にする。フェロモンを薄い経路として可視化。
- `slime-mold.html`: 餌・障害物を直接配置し、枝の成長、接続成功、最短化、障害物回避を大きく見せ、配置を変えて繰り返せる挑戦を設ける。
- `food-chain.html`: 生産者、被食者、捕食者を判別できる姿にし、場所への介入から捕食・個体群変化へつなぐ。
- `pillbug-maze.html`: 描いた迷路を複数のダンゴムシが進み、経路を比較できる状態を保持・改善。
- `monarch-migration.html`: 蝶、地理、食草、世代交代を判別可能にし、地図タップで渡りを助ける。
- `tree-rings.html`: 初期状態から年輪を触れ、干ばつ・大雨・火災を記録でき、早材・晩材・形成層が図で分かる。
- `weathering-lab.html`: 四角形ではなく岩として描き、割れ、欠け、水、根、粒の生成・運搬を直接操作で見せる。
- `fourier_drawing.html`: 手描きの線を回転円で再構成する別アプリとして保持し、小学生向け説明を備える。
- `paper-plane.html`: 飛行計算と指投げを保持し、初速、投射角、トリム、重心、翼面荷重、突風、迎角、滑空比を説明する。
- `simple-machines.html`: 滑車のロープ経路、力、荷物の動きを一目で分かるようにし、ロープまたは荷物を直接操作する。
- `cave.html`: 現在地、探索対象、発見経路を明示し、生物を画面内で見つけて収集する。
- `quicksand-simulator.html`: 沈下、浮力、もがき方の因果を大きく見せ、人物へ直接介入する。
- `fossil-formation.html`: 複数遺骸を同じ地層へ配置し、消失・部分保存・化石化を比較する。失敗でロックしない。
- `impact-deformation.html`: 落下位置を直接決め、衝突時自動スロー、変形前ゴースト、複数試験片比較を設ける。
- `whirlpool-strait.html`: 小舟・浮標を好きな位置へ投入し、潮位・舵で軌道を変える。
- `tanker-control.html`: 港内で進路、舵、逆進を直接指示し、「曲がらない・止まらない」を体感する。
- `airship-expedition.html`: 荷物、バラスト、ガス、風への直接介入で上昇・傾き・漂流を変える。
- `taxonomy-tree.html`: 共通祖先、時間、分岐、獲得形質、現生生物を判別可能に全面再設計する。
- `spider-web.html`: 支点・糸・獲物を直接配置し、捕獲・破断・逃走と設計トレードオフを示す。
- `river.html`: 任意地点タップによる川の断面図を復帰し、蛇行、外岸侵食、内岸堆積と同期させる。
- `raindrop-journey.html`: 雨粒を主役として大きく描き、段階と周囲条件を対応させ、雲・気流へ介入する。
- `engine-cycle.html`: クランク手回し、工程ごとの超低速、常時工程表示、点火時観察を設ける。
- `mirage-lab.html`: コントラストを修正し、地面温度、空気層、対象、観察者を直接操作して光路と像を変える。
- `material-fatigue.html`: タップで繰返し荷重を与え、拡大き裂、履歴、複数試験片、破断スローを見せる。
- `combustion.html`: 燃料、空気、水、壁への直接操作で炎、煙、不完全燃焼を変える。
- `snake.html`: 自動移動の観察ではなく、スワイプまたは画面方向タップで子ども自身がヘビを操作し、餌、成長、障害物、再挑戦を備えるゲームにする。
- `minesweeper.html`: 自動探索の観察ではなく、マスのタップで開く、長押しまたはモード切替で旗を立てる、勝敗後すぐ再挑戦できるゲームにする。
- `boids.html`: 空へタップして鳥を追加し、餌場・障害物・捕食者を置いて群れの分裂と再集合を起こせるようにする。鳥の数を主画面から増減できる。
- `fireflies.html`: 森の主画面へタップして木を任意に配置・移動・削除し、光の遮蔽による群れの分断と同期波の伝播を比較できるようにする。

## Exploration Apps

`abyss.html`, `cave.html`, `constellation-guide.html`, `fossil-formation.html`, `jungle.html`, `tidepool.html`を含む探検型を全件確認する。生物探検では、環境に合う生物が画面内に出現し、その個体をタップした時だけ一致する図鑑カードへ収集される方式へ統一する。汎用絵文字、探索ボタンだけでランダム追加、図鑑を開いて連打する方式は不可。見逃し、再出現、希少性、収集進捗を設ける。

## Shared UI Requirements

- リセットを「↺ はじめから」として一貫した位置へ置く。
- 色・記号の意味と主要用語を、主画面の近くに短く表示する。
- 子どもに意味のない`r69`、`r70`等の内部改訂番号を表示しない。
- 入口ページの文言は「おどろきとはっけんの実験室へようこそ」。
- 合言葉は`646450`。
- READMEにGitHub Pages URLを記載する。
- iPadで押しやすいサイズ、十分なコントラスト、動き軽減設定、キーボードフォーカスを保つ。

## Protected Successful Cores

- `element-sandbox.html`の砂・水・壁・木・火・煙・消しゴムと素材挙動。
- `sand.html`の粒状体コア。
- `paper-plane.html`の飛行計算と指で投げる操作。
- `fourier_drawing.html`の手描きと回転円再構成。

表示上の内部版番号除去、用語説明、アクセシビリティ改善は許可する。コア挙動を変更しない。

## Future Idea (Not in v1.1.0 Scope)

`docs/future_app_ideas.md`へ、下からの送風で羽・種・紙片を浮かせる「エアフロー・ホバリング」を記録する。v1.1.0完了後に検討し、今回実装しない。

## Validation and Release Gate

1. 183本、両カタログ183件、リンク切れなし。
2. 全ページを実ブラウザで読み込み、未処理例外0。
3. 各改修アプリの初期・再生・直接操作後を再取得し、主画面変化と意味のある因果を目視確認。
4. 入口なしと判定されたアプリは、題材固有の直接操作を追加するか、監査表へ合理的な適用除外理由を記す。
5. 共通タグの文字列`\n`が画面へ表示されない。
6. `node --check`、`bash -n`、`git diff --check`、リリース検査を通す。
7. 成功例の保護対象コアが意図せず変わっていない。
8. 変更を論理単位のConventional Commitsへ分ける。
9. `main`を`origin`へpushし、注釈付き`v1.1.0`タグをpushする。
10. push後にローカルHEAD、`origin/main`、`v1.1.0`が同じcommitを指すことを確認する。
